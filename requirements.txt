numpy>=1.26
pandas>=2.1
scikit-learn>=1.3
requests>=2.31
pytest>=8.0
# Windows has no built-in IANA timezone database; zoneinfo needs this
# package there for session-timezone handling. Harmless on other OSes.
tzdata>=2024.1
